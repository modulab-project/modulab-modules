# Build output — created by scripts/build-module.sh
dist/

# Cosign private key — never commit. See scripts/build-module.sh's header
# comment for one-time setup (cosign generate-key-pair). cosign.pub is the
# public half and is safe to share, but isn't needed in this repo - it gets
# embedded directly into modulab-core instead.
cosign.key
cosign.pub

# Module UI build artifacts — generated by npm run build
*/bundle/

# Node.js
*/ui/node_modules/
*/ui/package-lock.json
